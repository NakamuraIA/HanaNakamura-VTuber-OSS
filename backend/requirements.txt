# Tudo que a Hana precisa pra rodar. Um arquivo so, de proposito:
# o backend importa google-genai, groq e openai no topo dos modulos, entao
# "opcional" era mentira — sem eles o servidor nem sobe.

# --- API / servidor ---
fastapi>=0.110.0
uvicorn>=0.29.0
websockets>=12.0
httpx>=0.27.0
python-dotenv==1.2.1
psutil>=5.9.0

# --- MCP (ferramentas externas) ---
mcp>=1.27.1,<2

# --- Providers de LLM ---
google-genai>=1.68.0
groq==1.0.0
openai>=1.111.0

# --- Busca web ---
# Tavily entra via MCP com npx (mcp/config.py), nao pelo SDK Python.

# --- Voz (TTS/STT) ---
edge-tts==7.2.8
sounddevice==0.5.5
pygame==2.6.1
numpy==2.2.6

# --- Discord ---
discord.py[voice]>=2.4,<3
discord-ext-voice-recv==0.5.2a179
PyNaCl>=1.5,<2

# --- Visao / tela / teclado ---
Pillow==10.4.0
mss==9.0.1
keyboard==0.13.5
pynput==1.8.2

# --- Memoria semantica (so quando HANA_MEMORY_SEMANTIC=1) ---
fastembed>=0.8,<0.9
scikit-learn>=1.0

# --- Testes ---
pytest>=8.0
